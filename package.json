{
  "name": "agent-end-notify",
  "version": "1.0.0",
  "description": "Pi fleet notifications when agents end",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
